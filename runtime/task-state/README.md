# Task State

Derived local task indexes and disposable lifecycle caches may be generated
here. Canonical current task state lives in `projects/<name>/task.md`. Never
write state into the legacy external read-only tasks directory.
