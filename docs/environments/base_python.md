# Base Python

The workspace tools require Python 3.10 or newer. Python 3.10, 3.11, and 3.12
are checked on both Windows and Ubuntu.

Use a task-local virtual environment for project dependencies. The root workspace
tools use only the Python standard library; do not install task dependencies into
the root workspace.
