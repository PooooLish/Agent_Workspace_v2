# Environment Notes

This directory contains source-workspace environment notes that were classified
as Markdown guidance. It does not contain virtual
environments, credentials, caches, or downloaded dependencies.
