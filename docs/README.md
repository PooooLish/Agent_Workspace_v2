# Workspace Documentation

Durable framework decisions live under `framework/`; classified environment
guidance lives under `environments/`.

Simple work does not create standalone specifications or plans. Task-specific
designs remain with their owning task only after external task writes are
explicitly enabled. V2 does not maintain root-level Superpowers process
artifacts.

