# Claude Code Default Prompt

From a task directory, read `../../AGENTS.md`, local `AGENTS.md`, `README.md`, and
`task.md` first.

Constraints:

- Stay inside the current task directory unless told otherwise.
- Do not delete files.
- Do not store or reveal real API keys.
- Propose a short plan before editing.
- Prefer the smallest safe change.
- Finish with changed files, commands run, and verification result.
