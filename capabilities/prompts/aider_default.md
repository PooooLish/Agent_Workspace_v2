# Aider Default Prompt

From a task directory, read `../../AGENTS.md`, local `AGENTS.md`, `README.md`, and
`task.md` first.

Working rules:

- Do not delete files.
- Do not edit outside the current task unless asked.
- Do not store API keys or secrets in files.
- Start with a short plan.
- Make the smallest workable change.
- End with changed files, commands run, and verification result.
